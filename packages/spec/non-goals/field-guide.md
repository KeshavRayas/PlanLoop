# Field Guide — Non-Goals

packages/field-guide — Will NEVER:

- Import from `@planloop/browser-transport`
- Modify plans (read-only for planning)
- Implement protocol validation
- Make network calls
- Manage browser tabs
